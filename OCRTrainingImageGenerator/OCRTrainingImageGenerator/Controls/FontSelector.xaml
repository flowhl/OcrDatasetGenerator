<UserControl x:Class="OCRTrainingImageGenerator.Controls.FontSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="10,2"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="120"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Font Folder Selection -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Label Content="Font Folder:" Style="{StaticResource LabelStyle}"/>
            <TextBox Name="FontFolderPathBox" Style="{StaticResource TextBoxStyle}" Width="300" IsReadOnly="True"/>
            <Button Content="Browse..." Style="{StaticResource ButtonStyle}" Click="BrowseFontFolder_Click"/>
        </StackPanel>

        <!-- Font Preview Text Override -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <Label Content="Preview Text:" Style="{StaticResource LabelStyle}"/>
            <TextBox Name="FontPreviewTextBox" Style="{StaticResource TextBoxStyle}" Width="300" 
                     ToolTip="Leave empty to use 'The quick brown fox'" 
                     TextChanged="FontPreviewText_Changed"/>
        </StackPanel>

        <!-- Control Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
            <Button Content="Select All" Style="{StaticResource ButtonStyle}" Click="SelectAll_Click"/>
            <Button Content="Deselect All" Style="{StaticResource ButtonStyle}" Click="DeselectAll_Click"/>
            <TextBlock Name="StatusLabel" VerticalAlignment="Center" Margin="10,0,0,0" Text="No fonts loaded" FontStyle="Italic"/>
        </StackPanel>

        <!-- Fonts List -->
        <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="1" Margin="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl Name="FontsItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Checkbox -->
                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled}" 
                                              VerticalAlignment="Center" Margin="0,0,10,0"/>

                                    <!-- Font Name -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FileName}" FontWeight="Bold" FontSize="12"/>
                                        <TextBlock Text="{Binding FontFamilyName}" FontSize="10" Foreground="Gray"/>
                                    </StackPanel>

                                    <!-- Font Preview Image -->
                                    <Image Grid.Column="2" Source="{Binding PreviewImage}" 
                                           VerticalAlignment="Center" Margin="10,0,0,0"
                                           Stretch="None" HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>