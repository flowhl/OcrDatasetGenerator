<Window x:Class="OCRTrainingImageGenerator.Controls.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
        Title="Choose Color" 
        Height="600" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ColorPicker;component/Styles/DefaultColorPickerStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Label Grid.Row="0" 
               Content="Color Picker" 
               FontSize="16" 
               FontWeight="Bold" 
               HorizontalAlignment="Center" 
               Margin="10"/>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="20">
            <!-- Hex Input Section -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" 
                       Content="Hex Color:" 
                       Width="80" 
                       VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <colorpicker:HexColorTextBox x:Name="hexTextBox"
                                                Width="150"
                                                Height="25"
                                                VerticalAlignment="Center"
                                                ShowAlpha="True"
                                                HexRepresentation="ARGB"
                                                ColorState="{Binding ElementName=colorPicker, Path=ColorState, Mode=TwoWay}"/>
                </StackPanel>
            </Grid>

            <!-- Color Picker -->
            <colorpicker:StandardColorPicker x:Name="colorPicker"
                                            Width="420"
                                            Height="380"
                                            ShowAlpha="True"
                                            SmallChange="5"
                                            HexRepresentation="ARGB"
                                            Style="{StaticResource DefaultColorPickerStyle}"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="20">
            <Button Content="OK" 
                    Width="75" 
                    Height="30" 
                    Margin="5"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="Cancel" 
                    Width="75" 
                    Height="30" 
                    Margin="5"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>